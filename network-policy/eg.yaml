apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: egress-frontend
  namespace: default
spec:
  podSelector:
    matchLabels:
      deployment: frontend
  policyTypes:
  - Egress
  egress:
  - to:
    - podSelector:
        matchLabels:
          deployment: backend
